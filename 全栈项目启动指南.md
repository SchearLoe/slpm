# SLPM —— 全栈项目启动指南

> 液态毛玻璃生产力 OS · 智能任务与项目管理
> 第一期已完成：后端 + 认证 + 持久化 + 任务/日程核心闭环

---

## 📁 项目结构

```
D:\VibeCode\xmgl\
├── slpm-src\       # 前端 (React 19 + Vite + Tailwind v4)
├── slpm-server\    # 后端 (Express + Prisma + PostgreSQL)
├── SLPM-UI设计规范文档.md
├── 项目现状评估与功能建议.md
└── 全栈项目启动指南.md   ← 本文件
```

---

## 🛠 技术栈

| 层 | 技术 |
|----|------|
| 前端 | React 19 + TypeScript + Vite 6 + Tailwind CSS v4 + Framer Motion |
| 前端数据 | TanStack Query + axios |
| 前端路由 | React Router v6 |
| 后端 | Node.js + Express + TypeScript |
| ORM | Prisma |
| 数据库 | PostgreSQL 16（本机已安装运行） |
| 认证 | 邮箱密码 + JWT |

---

## 🚀 首次启动（3 步）

### 前提
- Node.js ≥ 20（本机 v22.14 ✅）
- PostgreSQL 16 运行中（本机 `localhost:5432` ✅，postgres 用户密码 `postgres`）
- 已建库 `slpm`（已建 ✅）

### 第 1 步：启动后端

```bash
cd D:\VibeCode\xmgl\slpm-server
npm install                 # 首次：装依赖
npx prisma migrate deploy   # 首次：应用数据库迁移（建表）
npm run dev                 # 启动，监听 http://localhost:8080
```

看到 `🟢 slpm-server 运行中: http://localhost:8080` 即成功。

> 若数据库连接失败，检查 `slpm-server/.env` 里 `DATABASE_URL` 的密码是否与你的 PostgreSQL 一致。

### 第 2 步：启动前端

```bash
cd D:\VibeCode\xmgl\slpm-src
npm install                 # 首次：装依赖
npm run dev                 # 启动，监听 http://localhost:3000
```

### 第 3 步：使用

1. 浏览器打开 **http://localhost:3000**
2. 自动跳转到登录页 → 点「注册」
3. 填邮箱/密码/姓名 → 注册 → 自动登录进入任务管理页
4. 开始创建任务、预约日程 —— **数据存数据库，刷新不丢失**

---

## ✅ 第一期已实现功能

| 功能 | 状态 |
|------|------|
| 用户注册/登录（邮箱密码 + JWT） | ✅ |
| 登录态持久化（localStorage + /auth/me 恢复） | ✅ |
| 路由守卫（未登录跳 /login） | ✅ |
| URL 持久化（/tasks、/schedule 等可分享） | ✅ |
| 任务 CRUD（新建/编辑/完成/删除）真实持久化 | ✅ |
| KPI 从真实任务实时聚合（非写死） | ✅ |
| 当前用户身份贯穿全站（清除 Brandon 硬编码） | ✅ |
| 日程 CRUD 真实持久化 | ✅ |
| 日程支持任意月份导航（修复原 demo 只能 5 月） | ✅ |
| 「我负责的/我参与的」按真实用户过滤 | ✅ |

---

## ⏳ 第二期待办（接口已预留）

- 文件真实上传（OSS/S3）
- 知识库 / 团队协作 持久化
- 多工作区 + 多成员权限（RBAC）
- 真实 AI 接入
- 通知系统（站内/邮件/WebSocket）

### ✅ 第二期已推进（P0 收尾）

| 功能 | 状态 |
|------|------|
| 主题设置持久化到 UserSettings（accentColor/glassBlur/enableConfetti） | ✅ |
| 设置页"保存全部"真实落库 + 刷新后主题恢复 | ✅ |
| confetti 开关真正生效（新建/完成任务时按设置控制） | ✅ |
| 项目总览页改真实数据聚合（任务完成率、阻塞点、阶段进度、成员负荷） | ✅ |
| 智能分析页改真实数据聚合（交付漏斗、成员效能、瓶颈阶段） | ✅ |
| 无数据源指标（代码提交数/AI采纳率/历史趋势）明确标注【演示】 | ✅ |

### ✅ 第二期已推进（P1-1 协作）

| 功能 | 状态 |
|------|------|
| 任务评论（发表/列表，支持 @姓名 解析高亮） | ✅ |
| 任务活动流（自动记录创建/更新/完成，时间倒序） | ✅ |
| 更新字段精确变更文案（如「优先级：中 → 高」） | ✅ |
| 活动流合并展示：评论以「带正文」形态出现一次（不重复） | ✅ |
| 任务删除级联清理评论/活动（onDelete: Cascade） | ✅ |

> **设计说明**：评论本身在活动流里以 `kind:'comment'`（带正文）形态展示，因此 `POST /comments` 不再额外写一条「发表评论」活动记录，避免同一评论在活动流里重复出现。`mentions` 字段已预留，为后续通知系统做准备。

### ✅ 第二期已推进（P1-2 多工作区 + RBAC）

| 功能 | 状态 |
|------|------|
| Workspace + WorkspaceMember 数据模型（多租户隔离） | ✅ |
| 注册自动建默认工作区「我的工作区」+ admin 成员 | ✅ |
| 旧数据自动迁移：每用户建默认工作区 + 任务/日程归属（backfill 脚本） | ✅ |
| 工作区上下文中间件（`X-Workspace-Id` header + 成员资格校验） | ✅ |
| 任务/日程按 workspaceId 隔离（非成员访问 → 403） | ✅ |
| RBAC 两档：admin（CRUD + 邀请/移除成员）/ member（CRUD 工作区内资源） | ✅ |
| 侧栏工作区切换真实化（移除硬编码，接 user.workspaces） | ✅ |
| 切换工作区即时刷新任务/日程（TanStack Query 失效） | ✅ |
| 邀请成员按角色禁用（member 看不到邀请按钮） | ✅ |

> **架构要点**：① 工作区 id 通过 `X-Workspace-Id` HTTP header 传输（不进 JWT，切换无需重登录）；② axios 拦截器自动注入；③ 后端 `requireWorkspace` 中间件校验成员资格并挂 `req.workspace = {id, role}`；④ `requireRole('admin')` 进一步做权限校验；⑤ Task/Schedule 的访问控制由 `workspaceId` 决定，`ownerId` 仅保留作"创建者"语义。

> **下一步（P1-3 候选）**：真实文件上传（OSS/S3，文件归档页目前仍内存态）、通知系统（站内/WebSocket，消费 `mentions`）、真实 AI 接入。

### ✅ 第二期已推进（P1-3 真实文件上传）

| 功能 | 状态 |
|------|------|
| FileRecord 模型 + multer multipart 解析 | ✅ |
| 文件存本地磁盘（uploads/<workspaceId>/，按工作区分目录） | ✅ |
| 上传 API（POST /api/files，20MB 限制） | ✅ |
| 下载 API（GET /api/files/:id/download，鉴权后返回文件流） | ✅ |
| 删除 API（DB 记录 + 磁盘文件同步清理） | ✅ |
| 文件按 workspaceId 隔离（跨工作区不可见/不可下载） | ✅ |
| 前端文件页接真实 API（移除内存态 initialFiles 硬编码） | ✅ |
| 真实 `<input type=file>` 上传 + multipart FormData | ✅ |
| 真实下载（blob 流 + 浏览器触发） + 真实删除 | ✅ |

> **存储架构**：multer diskStorage → `uploads/<workspaceId>/<uuid>.<ext>`，DB 存相对路径 `storagePath`。迁移到 S3/OSS 时只需替换 multer 的 storage backend（`multer-s3`）+ 调整下载接口，FileRecord 模型与 API 契约不变。`UPLOAD_DIR` 环境变量可配置存储根目录（默认 `uploads`）。

> **下一步（P1-4 候选）**：通知系统（站内/WebSocket，消费 `mentions`）、真实 AI 接入、文件预览（图片/PDF 内联渲染）、TeamCollaborationPage 成员列表接真实 workspace 成员、文件重命名接口。

### ✅ 第二期已推进（P1-4 真实 AI 接入）

| 功能 | 状态 |
|------|------|
| SystemConfig 模型（全局单例，存 AI baseURL/加密 Key/model/temp） | ✅ |
| 系统管理员角色（首个注册用户自动 system_admin）+ requireSystemAdmin 中间件 | ✅ |
| API Key AES-256-GCM 加密存储（AI_ENCRYPTION_KEY 派生密钥） | ✅ |
| 配置 API（GET 返回掩码不暴露明文；PUT 仅 system_admin） | ✅ |
| 连通性测试（POST /ai/test，用当前配置发测试消息） | ✅ |
| 任务智能建议代理（POST /ai/suggest，OpenAI 兼容 chat completions，30s 超时） | ✅ |
| 设置页管理员配置 UI（baseURL/Key/model/温度 + 保存 + 测试） | ✅ |
| 任务详情面板真实 AI 建议（替代写死字符串 + 假置信度） | ✅ |
| 智能分析页重算接真实 AI（替代 setTimeout 假重算） | ✅ |

> **架构要点**：① 前端 → 后端 `/api/ai/*` 代理 → 外部 LLM，**API Key 永不暴露给前端**（GET 只返回 `••••abcd` 掩码）；② OpenAI 兼容协议，支持任意兼容服务商（OpenAI / DeepSeek / Moonshot / 本地 Ollama 等），只需管理员填 baseURL + Key + model；③ 上游失败 → 502，超时(30s) → 504，未配置 → 400 引导提示；④ 模型返回 JSON 容错解析（```json 包裹 / 前后多余文本均能提取）。

> **配置入口**：设置中心 → AI 协同 → 顶部「AI 服务配置（管理员）」卡片（仅 system_admin 可见，非管理员看到提示）。第一个注册的用户自动是 system_admin；后续用户需手动 SQL 授权（`UPDATE "User" SET role='system_admin' WHERE email='...'`）。

> **下一步（P1-5 候选）**：文件预览（图片/PDF 内联渲染）、TeamCollaborationPage 成员列表接真实 workspace 成员、文件重命名接口、AI 流式响应（SSE）、token 用量统计。

### ✅ 第二期已推进（P1-5 五项合辑）

| 功能 | 状态 |
|------|------|
| **文件预览**：图片/PDF 内联渲染（blob URL，按 workspace 鉴权） | ✅ |
| **文件重命名**：PATCH /files/:id 更新 title（保留 originalName） | ✅ |
| **AI 流式响应**：POST /ai/suggest/stream SSE 逐 token 推送 | ✅ |
| **Token 用量统计**：AiUsageRecord 模型 + 近 30 天聚合 + 设置页真实用量卡片 | ✅ |
| **团队页增强**：成员卡片显示「已完成 N」统计 | ✅ |

> **架构要点**：① 文件 preview 路由用 `Content-Disposition: inline` + `fs.createReadStream` 管道输出（与 download 的 `res.download`（强制 attachment）互补）；② AI 流式调用 `callChatCompletionStream` 用 `fetch` + `ReadableStream` 解析 SSE delta，客户端断开时 `AbortController.abort()` 上游；③ stream_options: { include_usage: true } 让上游末尾返回 token 统计 → 落 AiUsageRecord；④ token 用量设置页展示 30 天聚合 + 迷你柱图（纯 CSS，无图表库）。

> **下一步（P1-6 候选）**：WebSocket 实时推送（替代通知轮询）、文件版本历史、自定义字段/工作流、移动端 PWA 适配、第三方集成。

> 总览/分析页中标注【演示】的指标需接入外部数据源（代码托管、测试平台、真实 AI）才能转为真实数据。

### ✅ 第二期已推进（P1-4 站内通知系统）

| 功能 | 状态 |
|------|------|
| Notification 数据模型（按【收件人 userId】维度，不绑定单个工作区） | ✅ |
| 评论 @姓名 触发通知（消费已存的 `TaskComment.mentions`，按姓名在工作区内解析为 userId） | ✅ |
| 任务指派变更触发通知（新建/编辑时 assigneeId 变化，含从无到有） | ✅ |
| 不打扰自己（给自己评论 / 指派给自己不发通知） | ✅ |
| 通知 API：列表 / 未读数 / 单条已读 / 全部已读 / 清除已读 | ✅ |
| 顶栏铃铛真实未读红点（30s 短轮询，替代原写死的 useState(3)） | ✅ |
| 通知弹窗接真实 API（移除原 5 条 seed 假数据 + "Brandon" 残留） | ✅ |
| 点击带 taskId 的通知 → 切换到对应工作区 + 跳转任务页（deep-link） | ✅ |
| 跨用户隔离（A 收不到 B 的通知） | ✅ |

> **架构要点**：① 通知按收件人维度，不走 `requireWorkspace`（顶栏铃铛应汇总用户在所有工作区的消息）；② 通知写入是"尽力而为"——`.catch(()=>{})` 吞错并记日志，绝不因通知写库失败而让评论/编辑主流程 500；③ 本期不做 WebSocket，前端用 TanStack Query 的 `refetchInterval: 30_000` 短轮询拉取未读数，体感近实时；④ `mentions` 仍是用户名（非 userId），发送时在工作区成员内按 `user.name` 解析，同名会给所有匹配者发。

> **下一步（P1-5 候选）**：真实 AI 接入、文件预览（图片/PDF 内联渲染）、文件重命名接口、WebSocket 实时推送（替代当前轮询）、通知偏好设置（按类型开关）。

### ✅ 第二期已推进（P1-3 知识库持久化 + 团队成员真实化）

| 功能 | 状态 |
|------|------|
| KnowledgeArticle 数据模型（workspaceId 隔离 + author 外键） | ✅ |
| 知识库 CRUD 真实持久化（发布/列表/详情/编辑/删除） | ✅ |
| 文章收藏切换（PG `starredByIds` 数组 + 乐观更新） | ✅ |
| 浏览量统计（打开详情自增，格式化 1.2k 显示） | ✅ |
| 发布文章作者自动取当前用户（清除写死 Brandon） | ✅ |
| TeamCollaborationPage 成员列表接真实 workspace 成员 | ✅ |
| 邀请成员改为按 email 邀请已注册用户（真实落库） | ✅ |
| 成员在办任务数从真实任务聚合（按 assigneeId 计数） | ✅ |
| admin 可改成员角色 / 移除成员（真实 API 调用） | ✅ |
| 切换工作区知识库/成员数据即时刷新（TanStack Query 失效） | ✅ |

> **残留**：① 文件归档页（FileDocumentsPage）仍内存态——需真实文件存储（OSS/S3）；② 发消息/发邮件仍是 Toast 演示——需通知系统（站内信/WebSocket）；③ 成员"在线状态"无数据源，已移除该字段，仅保留负荷饱和度演示值。

---

## 🔧 常用命令速查

### 后端（`slpm-server/`）

| 命令 | 作用 |
|------|------|
| `npm run dev` | 开发模式（tsx watch 热重载） |
| `npm run build` | 编译到 dist/ |
| `npm start` | 生产模式运行 |
| `npx prisma studio` | 可视化查看/编辑数据库 |
| `npx prisma migrate dev --name xxx` | 改 schema 后生成新迁移 |
| `npx prisma migrate reset` | 重置数据库（清空重建，慎用） |

### 前端（`slpm-src/`）

| 命令 | 作用 |
|------|------|
| `npm run dev` | 开发模式（端口 3000） |
| `npm run build` | 生产构建到 dist/ |
| `npm run preview` | 预览生产构建 |

---

## 🗄 数据库 Schema 概览

```
User          用户（邮箱密码 + 角色）
  ├─ UserSettings       主题/模糊/confetti 偏好
  ├─ WorkspaceMember[]  工作区成员关系（含 role: admin/member）
  ├─ Task[]             创建的任务（owner）
  ├─ Task[]             被指派的任务（assignee）
  └─ ScheduleEvent[]    日程

Workspace     工作区（名称/slug）
  └─ WorkspaceMember[]  成员（user + role）

Task          任务（标题/阶段/优先级/状态/截止/tags）
  ├─ workspaceId        所属工作区
  ├─ TaskComment[]      任务评论（作者/正文/mentions）
  └─ TaskActivity[]     活动流（操作者/action/detail）
ScheduleEvent 日程（标题/起止时间/地点/参会人 + workspaceId）
KnowledgeArticle 知识库文章（标题/正文/分类/浏览量/收藏/作者 + workspaceId）
FileRecord    文件记录（标题/原始名/mime/size/storagePath/分类/上传者 + workspaceId）
SystemConfig  系统配置（全局单例，AI baseURL/加密Key/model/temperature）
Notification  通知（type/title/body/taskId/read + userId 收件人 + workspaceId 来源）
```

> 完整定义见 `slpm-server/prisma/schema.prisma`

---

## 🔌 API 端点

> **P1-2 起**：除 `/api/auth` 和 `/api/workspaces` 自身外，所有资源接口都需要 `X-Workspace-Id` 请求头（由前端 axios 拦截器自动注入）。缺失或非成员 → 403。

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/api/auth/register` | 否 | 注册（自动建默认工作区 + admin） |
| POST | `/api/auth/login` | 否 | 登录 |
| GET | `/api/auth/me` | 是 | 取当前用户（含 settings + workspaces） |
| GET | `/api/workspaces` | 是 | 当前用户的工作区列表（含 role） |
| POST | `/api/workspaces` | 是 | 新建工作区（创建者自动 admin） |
| GET | `/api/workspaces/:id/members` | 是+WS | 工作区成员列表 |
| POST | `/api/workspaces/:id/members` | 是+WS+admin | 邀请成员（按 email） |
| PATCH | `/api/workspaces/:id/members/:userId` | 是+WS+admin | 改成员角色 |
| DELETE | `/api/workspaces/:id/members/:userId` | 是+WS+admin | 移除成员 |
| GET | `/api/tasks` | 是+WS | 任务列表（可过滤） |
| POST | `/api/tasks` | 是+WS | 新建任务 |
| PATCH | `/api/tasks/:id` | 是 | 编辑任务 |
| PATCH | `/api/tasks/:id/complete` | 是 | 完成任务 |
| DELETE | `/api/tasks/:id` | 是 | 删除任务 |
| GET | `/api/schedules?month=YYYY-MM` | 是 | 日程列表 |
| POST | `/api/schedules` | 是 | 新建日程 |
| PUT | `/api/schedules/:id` | 是 | 编辑日程 |
| DELETE | `/api/schedules/:id` | 是 | 删除日程 |
| GET | `/api/settings` | 是 | 取当前用户主题设置 |
| PUT | `/api/settings` | 是 | 更新主题设置 |
| GET | `/api/notifications` | 是 | 当前用户通知列表（最近 50 条） |
| GET | `/api/notifications/unread-count` | 是 | 未读通知数（顶栏红点用） |
| PATCH | `/api/notifications/:id/read` | 是 | 标记单条通知已读 |
| POST | `/api/notifications/read-all` | 是 | 全部标为已读 |
| DELETE | `/api/notifications/read` | 是 | 清除所有已读通知 |
| GET | `/api/files` | 是+WS | 工作区文件列表 |
| POST | `/api/files` | 是+WS | 上传文件（multipart，20MB 限制） |
| GET | `/api/files/:id/download` | 是+WS | 下载文件（鉴权后返回文件流） |
| DELETE | `/api/files/:id` | 是+WS | 删除文件（DB + 磁盘） |
| GET | `/api/ai/config` | 是+系统管理员 | 取 AI 配置（Key 返回掩码） |
| PUT | `/api/ai/config` | 是+系统管理员 | 更新 AI 配置（Key 加密落库） |
| POST | `/api/ai/test` | 是+系统管理员 | AI 连通性测试 |
| POST | `/api/ai/suggest` | 是+WS | 任务智能建议（OpenAI 兼容代理） |
| GET | `/api/tasks/:taskId/comments` | 是 | 任务评论列表（时间正序） |
| POST | `/api/tasks/:taskId/comments` | 是 | 发表评论（支持 @姓名 解析） |
| GET | `/api/tasks/:taskId/activity` | 是 | 任务活动流（评论+变更合并，时间倒序） |
| GET | `/api/articles` | 是+WS | 知识库文章列表（可过滤 category/starred） |
| POST | `/api/articles` | 是+WS | 新建文章（作者=当前用户） |
| GET | `/api/articles/:id` | 是+WS | 文章详情（浏览量 +1） |
| PATCH | `/api/articles/:id` | 是+WS | 编辑文章（title/body/category/pinned） |
| PATCH | `/api/articles/:id/star` | 是+WS | 切换当前用户对该文章的收藏 |
| DELETE | `/api/articles/:id` | 是+WS | 删除文章 |

---

## ❓ 常见问题

**Q: 启动后端报 `EADDRINUSE :::8080`？**
A: 端口被占用。查找并结束占用进程：
```bash
netstat -ano | grep ":8080" | grep LISTENING    # 找 PID
taskkill //PID <PID> //F                          # 结束
```

**Q: 想用 Docker 跑 PostgreSQL 而非本机原生？**
A: 见 `slpm-server/docker-compose.yml`，`docker compose up -d` 启动，并把 `.env` 的 `DATABASE_URL` 改为 `postgresql://slpm:slpm123@localhost:5432/slpm?schema=public`。

**Q: 忘记密码？**
A: 开发期直接用 Prisma 重置：`npx prisma studio` 打开 User 表，手动改 passwordHash（需先用 bcrypt 生成）。

**Q: 如何重置所有数据？**
A: `cd slpm-server && npx prisma migrate reset`（会清空所有表重建）。
