# SLPM —— 全栈项目启动指南

> 液态毛玻璃生产力 OS · 智能任务与项目管理
> 当前进度：P0 → P4-2 全部完成（含产品线、AI、WebSocket、E2E 测试）

---

## 📁 项目结构

```
D:\VibeCode\xmgl\
├── slpm-src\        # 前端 (React 19 + Vite 6 + Tailwind v4 + Framer Motion)
│   └── src\
│       ├── components/   # 布局 · 看板 · 弹窗 · UI 组件
│       ├── context/      # Auth · App 全局状态
│       ├── lib/          # api · queries · socket · aggregations · roleConfig
│       ├── pages/        # 10 页面路由（含产品管理/重置密码）
│       └── types/        # TypeScript 类型定义
├── slpm-server\     # 后端 (Express + Prisma + PostgreSQL + socket.io)
│   ├── src\
│   │   ├── config/       # 环境变量
│   │   ├── middleware/   # auth · workspace · product · admin · error
│   │   ├── routes/       # auth · task · schedule · file · ai · workspace ·
│   │   │                 # notification · settings · article · product ·
│   │   │                 # product-version · product-dashboard
│   │   └── lib/          # prisma · jwt · crypto · ws · notify · seed
│   └── prisma/           # schema.prisma + 13 个迁移
├── e2e\              # Playwright 冒烟测试（3 用例）
├── playwright.config.ts
├── README.md         # 完整项目文档（功能矩阵/API/架构决策）
├── CHANGELOG.md      # 按日期倒序的版本记录
└── SLPM-UI设计规范文档.md
```

---

## 🛠 技术栈

| 层 | 技术 |
|----|------|
| 前端 | React 19 + TypeScript + Vite 6 + Tailwind CSS v4 + Framer Motion |
| 前端数据 | TanStack Query + axios |
| 前端路由 | React Router v6 |
| 实时推送 | socket.io（客户端）+ socket.io-client（前端） |
| 后端 | Node.js + Express + TypeScript |
| ORM | Prisma（13 个迁移） |
| 数据库 | PostgreSQL 16（本机 localhost:5432） |
| 认证 | 邮箱密码 + JWT（bcrypt 哈希） |
| 文件存储 | multer 本地磁盘（uploads/，可平滑迁移 S3） |
| 测试 | Playwright（E2E 冒烟） |

---

## 🚀 首次启动（3 步）

### 前提
- Node.js ≥ 20（本机 v22 ✅）
- PostgreSQL 16 运行中（本机 `localhost:5432` ✅）
- 已建库 `slpm`（✅）

### 第 1 步：启动后端

```bash
cd D:\VibeCode\xmgl\slpm-server
npm install                 # 首次：装依赖
npx prisma migrate deploy   # 首次：应用全部数据库迁移（建表）
npm run dev                 # 启动，监听 http://localhost:8080
```

看到 `🟢 slpm-server 运行中: http://localhost:8080` 即成功。

> **超级管理员自动初始化**：启动时若数据库不存在 system_admin，会自动创建。默认账号 `admin@slpm.local` + **随机密码**（打印在控制台，安装人员可见）；也可在 `.env` 预设：
> ```
> INITIAL_ADMIN_EMAIL=admin@slpm.local
> INITIAL_ADMIN_PASSWORD=your-password
> ```

### 第 2 步：启动前端

```bash
cd D:\VibeCode\xmgl\slpm-src
npm install                 # 首次：装依赖
npm run dev                 # 启动，监听 http://localhost:3000
```

### 第 3 步：使用

1. 浏览器打开 **http://localhost:3000**
2. 注册/登录（首个注册用户自动成为系统管理员）
3. **首个注册用户会自动获得「演示项目」**：四阶段示例任务、里程碑、日程、知识库文章、欢迎通知（作为教程指引）
4. 开始使用：任务看板（可拖拽改阶段）、甘特图、日程、文件、AI 分析、产品管理…

> 存量数据库想补演示数据：`cd slpm-server && npm run seed:demo`（创建演示账号 demo@slpm.local / demo1234）

---

## ✅ 已实现功能总览

### 核心闭环（P0 → P1-6）
| 模块 | 内容 |
|------|------|
| 认证 | 注册/登录 + JWT + 登录态持久化 + 路由守卫 |
| 任务 | CRUD + 四阶段看板 + 拖拽改阶段 + 评论/活动流 + 任务依赖（父子/阻塞）+ 里程碑 + 甘特图 |
| 日程 | CRUD + 月/周/日三视图 + 任意月份 + **冲突预警** |
| 多工作区 | `X-Workspace-Id` 隔离 + RBAC（admin/pm/dev/qa/po）+ 侧栏切换 |
| 文件 | 上传/下载/删除/重命名/图片PDF预览/**版本历史与回滚** |
| 知识库 | 文章 CRUD + 收藏 + 浏览量 + 分类 |
| 通知 | @提及/指派/成员站内信 + WebSocket 实时推送 + 未读红点 + deep-link |
| AI | OpenAI 兼容代理 + 加密 Key 存储 + 流式 SSE + token 用量 + 规则推导分析 |
| 实时 | socket.io（JWT 握手 + workspace 房间）+ **在线状态绿点** |

### 角色化（P2-1 / P2-2）
- 职能角色：**PM / Dev / QA / 管理员 / 产品经理(PO)** —— 各角色有独立着陆页、导航排序、默认任务筛选、只读页面
- PM 专属看板：截止日预警 + 成员负荷分布 + 进度燃尽图

### 产品线（P3）
- **Product（产品线）→ Workspace（项目）→ Task** 三层架构
- 产品管理页 5 Tab：需求总览（跨项目聚合）/ **需求池**（排期/指派）/ **路线图**（版本时间轴）/ 版本管理（**Release Notes**）/ 团队视图
- **跨项目指派**：产品经理可直接给产品内任意项目成员派任务

### 系统初始化（P4-1）
- 超级管理员自动初始化（.env 配置或随机密码打印）
- 首用户演示数据种子 + `npm run seed:demo`
- 全站演示残留清理（假按钮/假数据全部真实化或移除）

### 工程化（P4-2 续）
- Playwright E2E 冒烟测试（3 用例：注册建任务 / 建日程 / 设置页）
- 忘记密码（15 分钟重置 token + 重置页）
- 真实头像上传（uploads/avatars/）

---

## 🔧 常用命令速查

### 后端（`slpm-server/`）

| 命令 | 作用 |
|------|------|
| `npm run dev` | 开发模式（tsx watch 热重载，端口 8080） |
| `npm run build` / `npm start` | 编译 / 生产运行 |
| `npx prisma migrate dev --name xxx` | 改 schema 后生成并应用迁移 |
| `npx prisma migrate deploy` | 应用未执行的迁移（部署用） |
| `npx prisma studio` | 可视化查看/编辑数据库 |
| `npm run seed:demo` | 手动播种演示数据（演示账号 demo@slpm.local / demo1234） |
| `npx prisma migrate reset` | 重置数据库（清空重建，慎用） |

### 前端（`slpm-src/`）

| 命令 | 作用 |
|------|------|
| `npm run dev` | 开发模式（端口 3000） |
| `npm run build` / `npm run preview` | 生产构建 / 预览 |

### E2E 测试（项目根目录）

| 命令 | 作用 |
|------|------|
| `npm run test:e2e` | Playwright 冒烟测试（需前后端已启动） |
| `npm run test:e2e:headed` | 有头模式运行（可视化调试） |

---

## 🗄 数据库 Schema 概览（13 个迁移）

```
User            用户（邮箱密码 + role: system_admin|成员 + avatar 头像）
  ├─ UserSettings      主题/模糊/confetti + 通知偏好（mention/assign/deadline）
  ├─ WorkspaceMember[] 工作区成员（role: admin|pm|dev|qa|po）
  ├─ ownedProducts[]   创建的产品线（ProductOwner）
  └─ …（任务/日程/评论/活动/通知/用量）

Product         产品线（name/slug/description/ownerId 负责人）
  ├─ Workspace[]       关联的项目（productId 可空，兼容独立工作区）
  └─ ProductVersion[]  版本（name/status[planning→released]/起止日期/order/releaseNotes）

Workspace       工作区（name/slug/productId）
  ├─ WorkspaceMember[] 成员
  ├─ Task[] / ScheduleEvent[] / KnowledgeArticle[] / FileRecord[] / Notification[]

Task            任务（四阶段/优先级/状态/截止/开始日期/里程碑/预估工时/版本）
  ├─ TaskComment[] / TaskActivity[]（评论与活动流）
  ├─ TaskDependency[]（阻塞依赖）/ children[]（父子层级）
  ├─ productVersionId  所属产品版本
  └─ workspaceId       所属工作区

FileRecord      文件（title/originalName/mime/size/storagePath/currentVersion）
  └─ FileVersion[]     版本历史（同名重传自动归档）

SystemConfig    全局单例（AI baseURL/加密Key/model/temperature）
Notification    通知（type: mention|assign|system + userId 收件人 + taskId deep-link）
AiUsageRecord   AI token 用量（endpoint/prompt/completion/total）
```

> 完整定义见 `slpm-server/prisma/schema.prisma`

---

## 🔌 API 端点

> 资源接口需要 `X-Workspace-Id` 请求头（axios 自动注入）；产品级接口需用户是该产品下任一项目成员。完整表格见 README。

| 模块 | 端点 |
|------|------|
| 认证 | POST `/auth/register` · `/auth/login` · `/auth/forgot-password` · `/auth/reset-password` · GET/PATCH `/auth/me` · POST `/auth/avatar` · GET `/auth/avatar/:file` |
| 工作区 | GET/POST `/workspaces` · `/workspaces/:id/members`（列表/邀请/改角色/移除） · GET `/workspaces/:id/online`（在线成员） |
| 任务 | GET/POST `/tasks` · PATCH `/tasks/:id` · `/tasks/:id/complete` · DELETE `/tasks/:id` · `/tasks/:id/comments` · `/tasks/:id/activity` |
| 日程 | GET `/schedules?month=` · POST/PUT/DELETE `/schedules/:id`（响应含 conflicts 冲突预警） |
| 文件 | GET/POST `/files` · GET `/files/:id/download` · `/files/:id/preview` · PATCH `/files/:id` · `/files/:id/versions` · POST `/files/:id/restore/:versionId` · DELETE |
| 知识库 | GET/POST `/articles` · GET/PATCH/DELETE `/articles/:id` · PATCH `/articles/:id/star` |
| 通知 | GET `/notifications` · `/notifications/unread-count` · PATCH `/:id/read` · POST `/read-all` · DELETE `/read` · POST `/send`（成员站内信） |
| 设置 | GET/PUT `/settings`（主题 + 通知偏好） |
| AI | GET/PUT `/ai/config`（system_admin） · POST `/ai/test` · POST `/ai/suggest` · `/ai/suggest/stream`（SSE） · GET `/ai/usage` |
| 产品 | GET/POST `/products` · GET/PATCH `/products/:id` · POST `/products/:id/workspaces` · DELETE `/products/:id/workspaces/:wsId` |
| 产品版本 | GET/POST `/products/:id/versions` · PATCH/DELETE `/products/:id/versions/:vid` |
| 产品聚合 | GET `/products/:id/tasks` · `/products/:id/members` · `/products/:id/stats` · PATCH `/products/:id/tasks/:taskId`（跨项目指派） |

---

## ❓ 常见问题

**Q: 启动后端报 `EADDRINUSE :::8080`？**
A: 端口被占用。查找并结束占用进程：
```bash
netstat -ano | grep ":8080" | grep LISTENING    # 找 PID
taskkill //PID <PID> //F                          # 结束
```

**Q: 想用 Docker 跑 PostgreSQL 而非本机原生？**
A: 见 `slpm-server/docker-compose.yml`，`docker compose up -d` 启动，并把 `.env` 的 `DATABASE_URL` 改为 `postgresql://slpm:slpm123@localhost:5432/slpm?schema=public`。

**Q: 忘记登录密码？**
A: 用登录页「忘记密码？」→ 输入邮箱 → 开发环境会在页面/服务端控制台给出重置链接（15 分钟内有效）→ 打开链接设置新密码。

**Q: 如何重置所有数据？**
A: `cd slpm-server && npx prisma migrate reset`（会清空所有表重建，随后启动会自动初始化超级管理员）。

**Q: 侧栏「产品线」没有内容？**
A: 需要先创建产品线（侧栏 + 按钮），再把项目（工作区）关联进去；产品管理页对 po/admin 显示全部聚合数据。

**Q: E2E 测试跑不起来？**
A: 先确保前后端已启动（8080/3000），再执行 `npm run test:e2e`；首次需 `npx playwright install chromium`。
